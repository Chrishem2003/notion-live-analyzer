NOTION_TOKEN = "your_notion_integration_token"
DATABASE_ID = "your_notion_database_id"

# Unlocks the professor-facing Forensic Audit view. The view stays locked
# while this is unset.
AUDIT_MASTER_PASSWORD = "choose_a_strong_password"

# JWT signing key for the Project Collaboration module. Set via the
# PROJECT_COLLAB_SIGNING_KEY environment variable in production; a random
# per-process key is used when it is missing.
